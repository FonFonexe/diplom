
1 Задание 2 части

SELECT login, "inDelivery"
FROM "Couriers" AS c
INNER JOIN "Orders" AS o ON c.id=o."courierId";


2 Задание 2 части

SELECT *,
CASE
    WHEN "finished" = 'true' THEN '2'
    WHEN "cancelled" = 'true' THEN '-1'
    WHEN "inDelivery" = 'true' THEN '1'
    ELSE '0'
END AS "Orders"
FROM "Orders";

или для вывода только одной колонки с статусами заказа

SELECT "Orders",
CASE
    WHEN "finished" = 'true' THEN '2'
    WHEN "cancelled" = 'true' THEN '-1'
    WHEN "inDelivery" = 'true' THEN '1'
    ELSE '0'
END AS "Orders"
FROM "Orders";
